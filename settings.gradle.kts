/*
 * Configuracion raiz del proyecto.
 * Se usan repositorios exclusivos de Google/Maven Central para garantizar
 * builds reproducibles y evitar dependencias transitivas no deseadas.
 */
pluginManagement {
    repositories {
        google {
            content {
                includeGroupByRegex("com\\.android.*")
                includeGroupByRegex("com\\.google.*")
                includeGroupByRegex("androidx.*")
            }
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "GestionIntegralSupermercados"
include(":app")
